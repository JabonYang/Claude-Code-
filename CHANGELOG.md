# Changelog

本文件记录 Feishu Claude Bot 的每次版本更新。

---

## v0.5.1 — 2026-06-10

### 改进
- **Ack 回复智能化**：确认回复从随机模板改为 Claude 实时生成，根据用户消息内容生成自然、贴切的回应，不再机械重复

## v0.5.0 — 2026-06-10

### 新增
- **两阶段执行**：任务执行前先由 Claude 评估可行性、预估耗时、检查是否有现成方案，评估结果以卡片形式发送，用户确认后再执行
- **流式进度推送**：执行阶段流式读取 stdout/stderr，每 60 秒发送一次进度提示
- **Plugin 自动加载**：动态发现并加载 `~/.claude/plugins/cache/` 下的所有 plugin
- **用户可控执行**：支持「开始执行」「取消」文本指令，移除 300 秒硬超时

### 配置
- 新增 `claude_progress_interval`、`claude_estimate_timeout` 配置项

## v0.4.0 — 2026-06-08

### 新增
- **macOS 开机自启**：添加 launchd service 配置，支持 macOS 系统启动时自动运行 Bot

## v0.3.0 — 2026-05-27

### 新增
- **合并转发消息支持**：识别并处理飞书合并转发（merge_forward）消息，通过 API 获取完整内容
- **上下文感知确认回复**：Bot 根据消息意图回复不同确认语（好的开始处理 / 正在查看 / 收到开始执行 / 让我想想）
- **多维表格 API**：新增 Bitable（多维表格）create/read/write 操作的 helper 方法

### 修复
- 超时后返回部分输出结果，不再丢失已产生的内容
- 错误信息改为中文，更友好
- 补全缺失的 `_decrypt` 和 `_get_tenant_token` 函数声明

## v0.2.2 — 2026-05-26

### 修复
- 补全缺失的 `_get_tenant_token` 函数声明（v0.3 中遗漏的回归修复）

## v0.2.1 — 2026-05-26

### 修复
- 抑制 Claude 主动追问的后续消息，避免在飞书中显示多余内容

## v0.2.0 — 2026-05-26

### 新增
- **WebSocket 断线重连**：指数退避重试（2s → 30s），不再因连接断开而静默退出
- **凭证校验**：连续 3 次快速断连后自动调用飞书 API 校验凭证有效性，凭证失效时停止线程并记录 CRITICAL 日志
- **系统休眠检测**：通过时间跳变监控（30 秒轮询）检测系统 sleep/wake，自动触发重连
- **热重载凭证**：新增 `POST /reload` 端点，无需重启即可更新凭证
- **增强健康检查**：`/health` 端点暴露线程状态和凭证状态

### 改进
- WebSocket 客户端新增 `validate_credentials()` 方法，支持 API 级别的认证检查

## v0.1.0 — 2026-05-25

### 首次发布

- **WebSocket 长连接**：通过飞书事件网关 WebSocket 接收消息，无需公网 URL
- **Claude Code CLI 集成**：完整工具链（文件读写、Bash 执行、代码搜索、Git 操作）
- **多轮对话**：每个飞书群/私聊独立会话，上下文持久保持
- **三层安全机制**：
  - 消息预扫描（匹配危险模式：系统文件删除、网络注入等）
  - Git 自动快照（每次执行前 `git add -A && git commit`）
  - Time Machine 本地快照兜底
- **内置命令**：回退、快照列表、回退到指定快照
- **FastAPI 服务**：提供 `/health` 健康检查端点
